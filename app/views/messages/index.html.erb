<p style="color: green"><%= notice %></p>

<h1>Greetings:</h1>

<div id="messages">
  <% @messages.each do |message| %>
    <%= render message %>
    <p>
      <%= link_to "Show this Greeting", message %>
    </p>
  <% end %>
</div>

<%= link_to "New greeting", new_message_path %>
